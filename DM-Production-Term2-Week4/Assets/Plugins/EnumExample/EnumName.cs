using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
///  A simple Enum example of an Enum called "EnumName" with 2 states.
/// </summary>

// Note: ACCESS_LEVEL - TYPE - NAME
// Define the enum, with each of the possible states listed.
public enum EnumName { state1 = 0, state2 = 1 };

